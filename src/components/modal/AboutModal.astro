---
import Modal from './Modal.astro';

export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<Modal
  id="about-modal"
  title="About Lucinda"
  size="lg"
  class={className}
>
  <div slot="description">
    Learn about Lucinda Burman's background, artistic journey, and creative philosophy.
  </div>

  <div slot="content" class="about-content">
    <div class="about-hero">
      <div class="about-hero__image">
        <!-- Profile image placeholder - replace with actual image -->
        <div class="about-profile-placeholder">
          <svg width="120" height="120" viewBox="0 0 120 120" fill="none" aria-hidden="true">
            <circle cx="60" cy="60" r="60" fill="#f5f4f2"/>
            <circle cx="60" cy="45" r="15" fill="#325c59"/>
            <path d="M30 95c0-16.5 13.5-30 30-30s30 13.5 30 30" fill="#325c59"/>
          </svg>
        </div>
      </div>
      <div class="about-hero__text">
        <h3 class="about-hero__name">Lucinda Burman</h3>
        <p class="about-hero__title">Illustrator, Designer & Creative Director</p>
        <p class="about-hero__location">Based in [Location]</p>
      </div>
    </div>

    <div class="about-section">
      <h4 class="about-section__title">My Story</h4>
      <div class="about-section__content">
        <p>
          With over a decade of experience in visual communication, I specialize in creating 
          compelling illustrations and designs that tell meaningful stories. My work spans 
          across editorial, commercial, and personal projects, always with a focus on 
          sophisticated aesthetics and thoughtful execution.
        </p>
        <p>
          I believe that great design is not just about making things look beautiful—it's 
          about creating connections, evoking emotions, and communicating ideas that resonate 
          with people on a deeper level.
        </p>
      </div>
    </div>

    <div class="about-section">
      <h4 class="about-section__title">Expertise</h4>
      <div class="about-section__content">
        <div class="expertise-grid">
          <div class="expertise-item">
            <h5>Illustration</h5>
            <p>Editorial, commercial, and conceptual illustration with a focus on narrative storytelling.</p>
          </div>
          <div class="expertise-item">
            <h5>Design Direction</h5>
            <p>Creative strategy and visual identity development for brands and publications.</p>
          </div>
          <div class="expertise-item">
            <h5>Art Direction</h5>
            <p>Leading creative teams and overseeing visual projects from concept to completion.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="about-section">
      <h4 class="about-section__title">Approach</h4>
      <div class="about-section__content">
        <p>
          My creative process begins with deep listening—understanding not just what needs 
          to be communicated, but how it should feel. I combine traditional techniques with 
          modern tools to create work that is both timeless and contemporary.
        </p>
        <p>
          Every project is an opportunity to explore new possibilities while maintaining 
          the highest standards of craftsmanship and attention to detail.
        </p>
      </div>
    </div>

    <div class="about-section">
      <h4 class="about-section__title">Let's Connect</h4>
      <div class="about-section__content">
        <p>
          I'm always interested in discussing new projects and creative collaborations. 
          Whether you're looking for illustration work, design direction, or creative consultation, 
          I'd love to hear about your vision.
        </p>
        <div class="about-cta">
          <button 
            type="button" 
            class="about-cta__button focusable" 
            data-modal-trigger="contact-modal"
            data-switch-modal="contact-modal"
          >
            Get in Touch
          </button>
        </div>
      </div>
    </div>
  </div>
</Modal>

<style>
  .about-content {
    max-width: 100%;
    line-height: var(--leading-relaxed);
  }

  .about-hero {
    display: flex;
    align-items: center;
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-xl);
    padding-bottom: var(--spacing-lg);
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }

  .about-hero__image {
    flex-shrink: 0;
  }

  .about-profile-placeholder {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    overflow: hidden;
    border: 2px solid rgba(50, 92, 89, 0.1);
  }

  .about-hero__text {
    flex: 1;
    min-width: 0;
  }

  .about-hero__name {
    font-family: var(--font-alt-heading);
    font-size: var(--text-xl);
    font-weight: 400;
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-xs);
    line-height: var(--leading-tight);
  }

  .about-hero__title {
    font-family: var(--font-alt-heading);
    font-size: var(--text-base);
    color: var(--color-primary);
    margin: 0 0 var(--spacing-xs);
    font-weight: 300;
  }

  .about-hero__location {
    font-family: var(--font-alt-heading);
    font-size: var(--text-sm);
    color: rgba(0, 0, 0, 0.6);
    margin: 0;
    font-weight: 300;
  }

  .about-section {
    margin-bottom: var(--spacing-lg);
  }

  .about-section:last-child {
    margin-bottom: 0;
  }

  .about-section__title {
    font-family: var(--font-alt-heading);
    font-size: var(--text-lg);
    font-weight: 400;
    color: var(--color-text-primary);
    margin: 0 0 var(--spacing-md);
    line-height: var(--leading-tight);
  }

  .about-section__content p {
    font-family: var(--font-alt-heading);
    font-size: var(--text-base);
    color: var(--color-text-primary);
    font-weight: 300;
    margin: 0 0 var(--spacing-md);
  }

  .about-section__content p:last-child {
    margin-bottom: 0;
  }

  .expertise-grid {
    display: grid;
    gap: var(--spacing-md);
    grid-template-columns: 1fr;
  }

  @media (min-width: var(--breakpoint-md)) {
    .expertise-grid {
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    }
  }

  .expertise-item h5 {
    font-family: var(--font-alt-heading);
    font-size: var(--text-base);
    font-weight: 400;
    color: var(--color-primary);
    margin: 0 0 var(--spacing-xs);
    line-height: var(--leading-tight);
  }

  .expertise-item p {
    font-family: var(--font-alt-heading);
    font-size: var(--text-sm);
    color: rgba(0, 0, 0, 0.7);
    font-weight: 300;
    margin: 0;
    line-height: var(--leading-normal);
  }

  .about-cta {
    margin-top: var(--spacing-md);
    padding-top: var(--spacing-md);
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }

  .about-cta__button {
    background: var(--color-primary);
    color: white;
    border: none;
    padding: var(--spacing-sm) var(--spacing-lg);
    border-radius: 2px;
    font-family: var(--font-alt-heading);
    font-size: var(--text-base);
    font-weight: 400;
    cursor: pointer;
    transition: all var(--transition-fast);
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .about-cta__button:hover {
    background: var(--color-primary-hover);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(50, 92, 89, 0.3);
  }

  .about-cta__button:focus {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }

  /* Mobile optimizations */
  @media (max-width: var(--breakpoint-sm)) {
    .about-hero {
      flex-direction: column;
      text-align: center;
      gap: var(--spacing-md);
    }

    .about-profile-placeholder {
      width: 100px;
      height: 100px;
    }

    .about-hero__name {
      font-size: var(--text-lg);
    }

    .about-section__title {
      font-size: var(--text-base);
    }

    .about-cta__button {
      width: 100%;
      padding: var(--spacing-md);
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .about-profile-placeholder {
      border-width: 3px;
    }

    .about-cta__button {
      border: 2px solid;
      font-weight: 600;
    }
  }

  /* Reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .about-cta__button {
      transition: none;
    }

    .about-cta__button:hover {
      transform: none;
    }
  }
</style>

<script>
  // Handle modal switching from About to Contact
  document.addEventListener('DOMContentLoaded', () => {
    const switchButtons = document.querySelectorAll('[data-switch-modal]');
    
    switchButtons.forEach(button => {
      button.addEventListener('click', (event) => {
        event.preventDefault();
        
        const targetModalId = button.getAttribute('data-switch-modal');
        if (!targetModalId) return;

        // Import modal manager functions
        import('../../scripts/modal-manager.js').then(({ closeModal, openModal }) => {
          // Close current modal and open target modal
          closeModal();
          
          // Small delay to allow first modal to close
          setTimeout(() => {
            openModal(targetModalId, button);
          }, 100);
        });
      });
    });
  });
</script>